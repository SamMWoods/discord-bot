services:
  lavalink:
    image: ghcr.io/lavalink-devs/lavalink:4
    container_name: lavalink
    restart: unless-stopped
    ports:
      - "2333:2333"
    volumes:
      - ./lavalink/application.yml:/opt/Lavalink/application.yml:ro
    env_file:
      - .env

  bot:
    build: .
    container_name: minionsbot
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - lavalink